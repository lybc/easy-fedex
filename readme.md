# 安装
编辑composer.json

```json
{
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/lybc/easy-fedex.git"
        }
    ],
    "require" : {
        "lybc/easy-fedex": "master-dev"
    }
}
```

执行命令
```bash
composer update
```